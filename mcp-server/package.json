{
  "name": "@gantry/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing gantry serve's instance-lifecycle, definitions-authoring, and workspace-lifecycle capabilities as tools, over streamable HTTP.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
