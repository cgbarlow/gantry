trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '22.x'
    displayName: 'Install Node.js 22'

  - script: sudo apt-get update && sudo apt-get install -y pandoc
    displayName: 'Install pandoc'

  - script: npm install
    displayName: 'Install dependencies'

  - script: npm test
    displayName: 'Run tests'

  - script: node bin/gantry.js render examples soap
    displayName: 'Render soap'

  - script: node bin/gantry.js render examples soap-full
    displayName: 'Render soap-full'

  - script: node bin/gantry.js render examples hld
    displayName: 'Render hld'

  - script: node bin/gantry.js render examples sad
    displayName: 'Render sad'

  - script: node bin/gantry.js render examples ssad
    displayName: 'Render ssad'

  - script: node bin/gantry.js render examples as-built
    displayName: 'Render as-built'
